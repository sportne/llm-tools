# llm-tools/.codex/config.toml

model = "gpt-5.4"  # main orchestrator
model_reasoning_effort = "high"

sandbox_mode = "workspace-write"
approval_policy = "never"
[agents]
max_threads = 20
max_depth = 2
